name: CI

on:
    push:
        branches: ['*']
    pull_request:
        branches: ['*']

jobs:
    build-and-test:
        name: Build, Lint, Type-Check & Test
        runs-on: ubuntu-latest
        strategy:
            matrix:
                node-version: [24.x]
        steps:
            # Checkout code
            - name: Checkout repository
              uses: actions/checkout@v4

            # Setup Node.js and cache dependencies
            - name: Setup Node.js ${{ matrix.node-version }}
              uses: actions/setup-node@v4
              with:
                  node-version: ${{ matrix.node-version }}
                  cache: 'npm'
                  cache-dependency-path: |
                      package-lock.json
                      grafana/plugins/governify-compliance-ranking-panel/package-lock.json

            # Install dependencies (clean install for CI)
            - name: Install dependencies
              run: npm ci

            - name: Install and build Grafana plugin
              run: |
                  npm run plugin:install
                  npm run plugin:build

            # Lint only src/ for TypeScript, markdown anywhere
            - name: Lint code
              run: npm run lint

            # Check code formatting
            - name: Check formatting
              run: npm run format:check

            # TypeScript strict type-check (no emit)
            - name: TypeScript type-check
              run: npx tsc --noEmit

            # Run tests with coverage
            - name: Run tests
              run: npm test
              env:
                  CI: true

            - name: Build both container images
              run: npm run images:build
